export class GroupApplyService{
    // 邀请列表
    static async getList(groupId: number) {
    }
    // 同意邀请
    static async acceptInvite(groupId: number) {
    }
    // 创建进群请求
    static async create(groupId: number) {
    }
    // 拒绝邀请
    static async refuseInvite(gid: number) {
    }
}